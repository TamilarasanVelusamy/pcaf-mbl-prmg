import XCTest
@testable import PepNetworkingPackage

final class PepNetworkingPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PepNetworkingPackage().text, "Hello, World!")
    }
}
